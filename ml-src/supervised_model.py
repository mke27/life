import numpy as np
import pandas as pd

def lin_reg(X, y):
    """returns weights in an arrary of running the regression model.
        Args:
            X (array): array of input features.
            y (array): array of real outputs
    """